spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog_study
    username: root
    password: e8047967

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      # shoq_sql: true
      format_sql: true
